html = open("/dev/stdin").read() if False else None

# Leer el HTML del documento (lo escribo inline aquí directamente)
# En vez de eso, voy a aplicar sed directamente sobre el archivo que crearé

print("script listo")
